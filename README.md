# Calculator in C

This is a simple calculator program written in C. It can perform basic arithmetic operations like addition, subtraction, multiplication, and division.

## How to Run

Compile the `calculator.c` file using a C compiler and run the resulting executable.
